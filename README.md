# OpenApSharp

A C# / .NET translation of select parts of the python library [OpenAP](https://github.com/TUDelft-CNS-ATM/openap).

The original python library is needed (for its data files) as a submodule in the project root.

## Building and Running

```sh
dotnet build
dotnet test
```
